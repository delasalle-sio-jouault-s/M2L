<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="simon.android.m2l">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuGeneral" />
        <activity android:name=".ConsulterReservations" />
        <activity android:name=".ConsulterSalles" />
        <activity android:name=".ConfirmerReservation" />
        <activity android:name=".AnnulerReservation" />
        <activity android:name=".ChangerMdp" />
        <activity android:name=".CreerUtilisateur" />
        <activity android:name=".SupprimerUtilisateur" />
    </application>

</manifest>
